import React from 'react';
// import { useEffect, useState } from 'react';
// import { useAuthState } from 'react-firebase-hooks/auth';
// import { Link, Navigate } from 'react-router-dom';
// import auth from "../../../Firebase/Firebase.init";
// import useInventories from '../../../hooks/useInventories';


const MysingleInventory = () => {
    // const [item, setItem] = useState();
    // const [user] = useAuthState(auth);
    // const {singleInventory} = props;
    // const newI =[singleInventory];
    // const [inventories, setInventories] = useInventories([]);
    // const singleItem = inventories.filter(items => items.email === user?.email);
    // console.log(singleItem);
    // setInventories(singleItem);

    
    return (
        <div>
            {/* <h1>Name: {setInventories?.length}</h1> */}
        </div>
    );
};

export default MysingleInventory;